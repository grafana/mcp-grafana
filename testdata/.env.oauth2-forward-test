
# ============================================================
# OAuth2 Configuration for MCP Grafana Server (Token Forwarding)
# ============================================================
# Enable OAuth2 token validation for incoming requests.
export OAUTH2_ENABLED=true

# Keycloak OAuth2 provider URL for local testing via published docker-compose port.
export OAUTH2_PROVIDER_URL=http://localhost:8082/realms/mcp-grafana

# OAuth2 User Info endpoint used by MCP for token validation.
export OAUTH2_USERINFO_ENDPOINT=/protocol/openid-connect/userinfo

# Token cache TTL in seconds.
export OAUTH2_TOKEN_CACHE_TTL=300

# ============================================================
# OAuth2 Token Forwarding to Grafana (Enabled by Default)
# ============================================================
# Token forwarding is ENABLED BY DEFAULT when OAuth2 is enabled.
# MCP automatically validates and forwards user tokens to Grafana.
# 
# Default mode: Bearer token forwarding
#   Forwards: Authorization: Bearer <validated-user-token>
#   Suitable for: Self-hosted Grafana
#
# Optional below: Uncomment to use Grafana Cloud-style headers instead
#   Forwards: X-Access-Token: <service-token> + X-Grafana-Id: <user-token>
#   Suitable for: Grafana Cloud

# Use Cloud-style headers for Grafana Cloud (optional)
# export OAUTH2_TOKEN_FORWARD_TO_GRAFANA_USE_CLOUD_HEADERS=true
# export GRAFANA_API_KEY=<grafana-service-account-token>

# ============================================================
# Grafana Configuration
# ============================================================
export GRAFANA_URL=http://localhost:3000
export GRAFANA_ORG_ID=1

# Optional: Grafana API key for cloud-header mode or fallback
# Not needed for bearer token forwarding mode
# export GRAFANA_API_KEY=

# ============================================================
# Auth Proxy Configuration (Optional - Disabled by Default)
# ============================================================
# Auth Proxy is DISABLED BY DEFAULT.
# Enable only if you want Grafana to manage additional user session context.
# Note: Token forwarding (above) and Auth Proxy are independent features.
#
# Uncomment below to enable Auth Proxy headers:
#
# export GRAFANA_PROXY_AUTH_ENABLED=true
# export GRAFANA_PROXY_USER_HEADER=X-WEBAUTH-USER
# export GRAFANA_PROXY_EMAIL_HEADER=X-WEBAUTH-EMAIL
# export GRAFANA_PROXY_NAME_HEADER=X-WEBAUTH-NAME
# export GRAFANA_PROXY_ROLE_HEADER=X-WEBAUTH-ROLE

# ============================================================
# MCP Server Configuration
# ============================================================
export MCP_SERVER_MODE=sse
export MCP_SERVER_PORT=8080
export MCP_SERVER_ADDRESS=localhost:8080

# Disable proxied tools for per-request OAuth2 user contexts.
export MCP_DISABLE_PROXIED_TOOLS=true

# ============================================================
# Logging
# ============================================================
export LOG_LEVEL=debug

# ============================================================
# Usage
# ============================================================
# source testdata/.env.oauth2-forward-test
# go run ./cmd/mcp-grafana/main.go
